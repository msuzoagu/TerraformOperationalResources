AWSTemplateFormatVersion: '2010-09-09'
Description: creates policy that grants principals in a user management account permission to create resources in workload accounts

Parameters:
  GroupName:
    Type: String
  PolicyName:
    Type: String
  TfStateRoleName:
    Type: String
  WorkloadAccountId:
    Type: String
  ResourceAccessorAccountId:
    Type: String  


Rules:
  Deployment:
    Assertions:
      - Assert: !Equals
        - !Ref AWS::AccountId
        - !Ref ResourceAccessorAccountId
        AssertDescription: 'must be deployed in UserMgtAccount account'


Resources:
  GroupPolicy:
    Type: 'AWS::IAM::ManagedPolicy'
    DeletionPolicy: Delete
    UpdateReplacePolicy: Retain
    Properties:
      Groups: 
        - !Ref GroupName
      ManagedPolicyName: !Ref PolicyName
      Description: !Sub "grants permission to CRUD resources using Terraform by assuming ${TfStateRoleName} in workload accounts"
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Sid: CRUDResourcesViaTerraform
            Effect: Allow
            Action:
              - 'sts:AssumeRole'
            Resource:
              - !Sub "arn:aws:iam::${WorkloadAccountId}:role/${TfStateRoleName}"
  